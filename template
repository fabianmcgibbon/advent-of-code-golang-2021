package main

import (
	"adventofcode/utils/files"
)

func main() {
	input := files.ReadFile(x, "\n")
	solution := solvePartX(input)
	fmt.Println(solution)
}

func solvePartX(input []string) (result int) {

    return result
}
